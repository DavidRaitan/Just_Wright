version: "3.9"

services:
  just-wright:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/just_wright.db
    restart: unless-stopped
